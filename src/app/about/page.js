import AboutSection from '@/components/AboutSection/AboutSection';

export const metadata = {
  title: 'About — Mikael Cheung',
  description: 'UX/product designer based in Toronto.',
};

export default function AboutPage() {
  return <AboutSection />;
}
